apiVersion : v1
kind: Service
metadata:
    name: "schemakube-4b8d"
    labels:
        app: "schemakube-4b8d"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: app: "schemakube-4b8d"
        name : app: "schemakube-4b8d"
